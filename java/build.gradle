plugins {
    id 'java-library'
}

afterEvaluate {
    publishing {
        publications {
            "$project.name"(MavenPublication) {
                from components.java

                groupId = "$group_id"
                artifactId = project.name
                version = project.version
            }
        }
    }
}